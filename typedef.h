#ifndef TD
#define TD

#include <iostream>
#include <fstream>
#include <vector>
#include <list>
#include <map>

typedef std::map<char, int> FreqMap;
typedef std::map<char, std::vector<bool>> huffmanMap;
typedef std::map<std::vector<bool>, char> reverseHuffmanMap;

#endif